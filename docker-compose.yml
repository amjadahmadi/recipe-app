version: '3.9'

services:
  app:
    build:
      context: .
      args:
         - ENV=dev
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    environment:
      - DEBUG=1
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"